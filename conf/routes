# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index()
GET		/forms						controllers.Application.forms()
GET		/logout						controllers.Application.logout()

GET		/clearSavedChoices			controllers.FormFill.clearSavedChoices()
GET		/form						controllers.FormFill.getForm()
POST	/next						controllers.FormFill.next()
POST	/prev						controllers.FormFill.prev()
GET		/getFormOutput				controllers.FormFill.getFormOutput()

POST    /authenticate               controllers.Application.authenticate()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
